import {ReactNode} from 'react'

interface Props {
  value: number
  label: ReactNode
  color?: string
}

export const HorizontalBarChartItem = ({value, label, color}: Props) => {
  return <div></div>
}
