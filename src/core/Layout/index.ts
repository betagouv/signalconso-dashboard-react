export const layoutConfig = {
  headerHeight: 64,
  sidebarWidth: 240,
  headerPx: 2,
}

export { LayoutContextProvider as LayoutProvider } from '../context/LayoutContext'
export { Layout } from './Layout'
